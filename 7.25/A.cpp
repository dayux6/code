#include <bits/stdc++.h>

int main()
{
    int n, m;
    std::cin >> n >> m;
    std::pair<int, int> *pr = new std::pair<int, int>[n];
    for (int i = 0; i < n; ++i)
        std::cin >> pr[i].first >> pr[i].second;
    int *suf = new int[n];
    
    return 0;
}