#include <bits/stdc++.h>

int main()
{
    freopen("B.in", "r", stdin);
    int n;
    std::cin >> n;
    for (int i = 0; i < n; ++i)
        putchar('g');
    for (int i = 0; i < n; ++i)
        putchar('z');
    putchar('g');
    return 0;
}